# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ....core.pydantic_utilities import UniversalBaseModel
from ....core.serialization import FieldMetadata


class DashboardPlacement_Widget(UniversalBaseModel):
    """
    A tile on the dashboard's 12-column grid. `x`/`y` are the tile's
    top-left cell (0-based; `y` grows downward), `width`/`height` its size
    in cells. The UI default tile is 6x6 (half width). Overlapping tiles
    are not rejected; prefer appending below existing tiles (or omit the
    position on create to let the server do it).
    """

    type: typing.Literal["widget"] = "widget"
    id: str
    widget_id: typing_extensions.Annotated[str, FieldMetadata(alias="widgetId")]
    x: int
    y: int
    width: int
    height: int

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(
        extra="allow", frozen=True
    )


class DashboardPlacement_Preset(UniversalBaseModel):
    """
    A tile on the dashboard's 12-column grid. `x`/`y` are the tile's
    top-left cell (0-based; `y` grows downward), `width`/`height` its size
    in cells. The UI default tile is 6x6 (half width). Overlapping tiles
    are not rejected; prefer appending below existing tiles (or omit the
    position on create to let the server do it).
    """

    type: typing.Literal["preset"] = "preset"
    id: str
    preset_id: typing_extensions.Annotated[str, FieldMetadata(alias="presetId")]
    x: int
    y: int
    width: int
    height: int

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(
        extra="allow", frozen=True
    )


DashboardPlacement = typing_extensions.Annotated[
    typing.Union[DashboardPlacement_Widget, DashboardPlacement_Preset],
    pydantic.Field(discriminator="type"),
]
